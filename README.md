# Bounded Autonomy (H0 Baseline)
Ungoverned baseline simulator for AI agent action execution.
No governance, no LLM, no external dependencies. Pure Python.

## Setup
python3 -m venv .venv
source .venv/bin/activate

## Run Tests (H0 Baseline)
python -m unittest discover -s tests -v
